# AWS-Powered-Face-Recognition-Attendance-System
A hybrid edge-cloud attendance management system built with Flask and Python, using OpenCV for sub-second local facial recognition and AWS services (S3 and DynamoDB) for scalable asset storage, real-time sync, and offline database fallback.
